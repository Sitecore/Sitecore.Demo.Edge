export interface Blog {
  id: string;
  blog_Title: string;
  blog_Quote: string;
  blog_Body: string;
}

export interface Timeslot {
  id: string;
  name: string;
}

export interface Params {
  id: string
}